# ===========================================
# AZURE PRODUCTION ENVIRONMENT VARIABLES
# ===========================================
# Copy these to Azure App Service Configuration
# Portal → App Service → Configuration → Application settings
# ===========================================

# -----------------------------------------
# REQUIRED - Application will NOT work without these
# -----------------------------------------

NODE_ENV=production
PORT=8080

# Database (get from Azure Portal → PostgreSQL → Connection strings)
DATABASE_URL=postgresql://USERNAME:PASSWORD@SERVERNAME.postgres.database.azure.com:5432/stargate_portal?sslmode=require

# Security Keys (generate unique values!)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=GENERATE_64_CHAR_RANDOM_STRING_HERE
ENCRYPTION_KEY=GENERATE_32_CHAR_RANDOM_STRING_HERE

# -----------------------------------------
# AI SERVICES - At least ONE required for website generation
# -----------------------------------------

# OpenAI (RECOMMENDED - Primary AI for website generation)
OPENAI_API_KEY=sk-your-openai-key

# Anthropic Claude (RECOMMENDED - For Merlin chat)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google Gemini (Optional but recommended)
GEMINI_API_KEY=your-gemini-key

# -----------------------------------------
# IMAGE GENERATION - At least ONE required
# -----------------------------------------

# Leonardo AI (RECOMMENDED for website images)
LEONARDO_AI_API_KEY=your-leonardo-key

# Replicate (Alternative image generation)
REPLICATE_API_TOKEN=r8_your-replicate-token

# Stock Photos (FREE - highly recommended)
UNSPLASH_ACCESS_KEY=your-unsplash-key

# -----------------------------------------
# OPTIONAL - Enhanced Features
# -----------------------------------------

# Payments (for premium features)
STRIPE_SECRET_KEY=sk_live_your-stripe-key
STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-key
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-key

# Email Notifications
SENDGRID_API_KEY=SG.your-sendgrid-key
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# Deployment
VERCEL_API_TOKEN=your-vercel-token

# -----------------------------------------
# AZURE SPECIFIC
# -----------------------------------------

# App Service URL (update after deployment)
FRONTEND_URL=https://your-app-name.azurewebsites.net
VITE_API_URL=https://your-app-name.azurewebsites.net
ALLOWED_ORIGINS=https://your-app-name.azurewebsites.net

# WebSocket URL
VITE_SOCKET_URL=wss://your-app-name.azurewebsites.net
